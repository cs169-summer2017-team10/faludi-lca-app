angular
    .module('focusLcaApp')
    .controller('HomeCtrl', [
        '$scope',
        '$rootScope',
        'Auth',
        function($scope, $rootScope, Auth){
            // $scope.hello = "Hello World";
        }]);
